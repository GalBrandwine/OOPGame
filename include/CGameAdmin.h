#pragma once

/**
 * @brief This is game admin,
 * The it initiate the game, load settings and start the Game loop.
 * 
 */
class CGameAdmin
{
private:
    /* data */
public:
    CGameAdmin(/* args */);
    ~CGameAdmin();
};
